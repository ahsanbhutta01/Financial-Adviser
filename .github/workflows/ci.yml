name: CI Pipeline - Build & Push to Docker Hub

on:
  push:
    branches: [ deploy-finadv ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
  pull_request:
    branches: [ deploy-finadv ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'

env:
  REGISTRY: docker.io
  IMAGE_NAME_CLIENT: hassan915/financial-adviser-client
  IMAGE_NAME_SERVER: hassan915/financial-adviser-server

jobs:
  # Job 1: Test and Build Client (React App)
  client-build:
    name: Build and Test Client
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Check for client changes
      uses: dorny/paths-filter@v2
      id: client-changes
      with:
        filters: |
          client:
            - 'client/**'
            - 'package.json'
            - '.github/workflows/ci.yml'
    
    - name: Setup Node.js
      if: steps.client-changes.outputs.client == 'true'
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: client/package-lock.json
        
    - name: Cache client node_modules
      if: steps.client-changes.outputs.client == 'true'
      uses: actions/cache@v3
      with:
        path: client/node_modules
        key: ${{ runner.os }}-client-${{ hashFiles('client/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-client-
        
    - name: Install client dependencies
      if: steps.client-changes.outputs.client == 'true'
      working-directory: ./client
      run: npm ci
      
    - name: Run ESLint
      if: steps.client-changes.outputs.client == 'true'
      working-directory: ./client
      run: npx eslint . --ext .js,.jsx,.ts,.tsx --max-warnings 0
      continue-on-error: false
      
    - name: Build client
      if: steps.client-changes.outputs.client == 'true'
      working-directory: ./client
      run: npm run build
      
    - name: Upload client build artifacts
      if: steps.client-changes.outputs.client == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: client-build
        path: client/dist/
        retention-days: 1
        
    outputs:
      client-changed: ${{ steps.client-changes.outputs.client }}

  # Job 2: Test and Build Server (Node.js API)
  server-build:
    name: Build and Test Server
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Check for server changes
      uses: dorny/paths-filter@v2
      id: server-changes
      with:
        filters: |
          server:
            - 'server/**'
            - 'package.json'
            - '.github/workflows/ci.yml'
      
    - name: Setup Node.js
      if: steps.server-changes.outputs.server == 'true'
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: server/package-lock.json
        
    - name: Cache server node_modules
      if: steps.server-changes.outputs.server == 'true'
      uses: actions/cache@v3
      with:
        path: server/node_modules
        key: ${{ runner.os }}-server-${{ hashFiles('server/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-server-
        
    - name: Install server dependencies
      if: steps.server-changes.outputs.server == 'true'
      working-directory: ./server
      run: npm ci
      
    - name: Check server syntax
      if: steps.server-changes.outputs.server == 'true'
      working-directory: ./server
      run: node --check server.js
      
    outputs:
      server-changed: ${{ steps.server-changes.outputs.server }}

  # Job 3: Build and Push Docker Images
  docker-build-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    needs: [client-build, server-build]
    if: github.event_name == 'push' && github.ref == 'refs/heads/deploy-finadv' && (needs.client-build.outputs.client-changed == 'true' || needs.server-build.outputs.server-changed == 'true')
    permissions:
      contents: read
      packages: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - name: Extract metadata for client
      if: needs.client-build.outputs.client-changed == 'true'
      id: meta-client
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_CLIENT }}
        tags: |
          type=ref,event=branch
          type=sha,prefix=sha-
          type=raw,value=latest,enable={{is_default_branch}}
        
    - name: Extract metadata for server
      if: needs.server-build.outputs.server-changed == 'true'
      id: meta-server
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_SERVER }}
        tags: |
          type=ref,event=branch
          type=sha,prefix=sha-
          type=raw,value=latest,enable={{is_default_branch}}
        
    - name: Build and push client Docker image
      if: needs.client-build.outputs.client-changed == 'true'
      uses: docker/build-push-action@v5
      with:
        context: ./client
        file: ./client/Dockerfile
        push: true
        tags: ${{ steps.meta-client.outputs.tags }}
        labels: ${{ steps.meta-client.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    - name: Build and push server Docker image
      if: needs.server-build.outputs.server-changed == 'true'
      uses: docker/build-push-action@v5
      with:
        context: ./server
        file: ./server/Dockerfile
        push: true
        tags: ${{ steps.meta-server.outputs.tags }}
        labels: ${{ steps.meta-server.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  # Job 4: Security Scan
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [client-build, server-build]
    if: needs.client-build.outputs.client-changed == 'true' || needs.server-build.outputs.server-changed == 'true'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install npm-audit
      run: npm install -g npm-audit-resolver
      
    - name: Run security audit - Client
      if: needs.client-build.outputs.client-changed == 'true'
      working-directory: ./client
      run: npm audit --audit-level moderate
      continue-on-error: true
      
    - name: Run security audit - Server
      if: needs.server-build.outputs.server-changed == 'true'
      working-directory: ./server
      run: npm audit --audit-level moderate
      continue-on-error: true

  # Job 5: Code Quality Check
  code-quality:
    name: Code Quality Check
    runs-on: ubuntu-latest
    needs: [client-build, server-build]
    if: needs.client-build.outputs.client-changed == 'true' || needs.server-build.outputs.server-changed == 'true'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install client dependencies
      if: needs.client-build.outputs.client-changed == 'true'
      working-directory: ./client
      run: npm ci
      
    - name: Run client linting
      if: needs.client-build.outputs.client-changed == 'true'
      working-directory: ./client
      run: npm run lint
      
    - name: Check for TODO/FIXME comments
      run: |
        echo "Checking for TODO/FIXME comments..."
        if grep -r "TODO\|FIXME" --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" client/ server/ || true; then
          echo "Found TODO/FIXME comments - consider addressing them"
        fi

  # Job 6: CI Pipeline Summary
  ci-cd-summary:
    name: CI Pipeline Summary
    runs-on: ubuntu-latest
    needs: [client-build, server-build, docker-build-push, security-scan, code-quality]
    if: always()
    
    steps:
    - name: CI Pipeline Results Summary
      run: |
        echo "## � CI Pipeline Results Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🛠️ Build & Test Results" >> $GITHUB_STEP_SUMMARY
        echo "- **Client Build**: ${{ needs.client-build.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Server Build**: ${{ needs.server-build.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Docker Build & Push**: ${{ needs.docker-build-push.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Security Scan**: ${{ needs.security-scan.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Code Quality**: ${{ needs.code-quality.result }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [[ "${{ needs.client-build.result }}" == "success" && "${{ needs.server-build.result }}" == "success" && "${{ needs.docker-build-push.result }}" == "success" ]]; then
          echo "✅ **CI Pipeline Success! Docker images built and pushed.**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "� **Docker Images Available on Docker Hub:**" >> $GITHUB_STEP_SUMMARY
          echo "- \`hassan915/financial-adviser-client:${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`hassan915/financial-adviser-server:${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🚀 **Next Steps:**" >> $GITHUB_STEP_SUMMARY
          echo "- Images are ready for manual deployment" >> $GITHUB_STEP_SUMMARY
          echo "- Use \`docker-compose.prod.yml\` for production deployment" >> $GITHUB_STEP_SUMMARY
          echo "- Add GitHub Secrets for environment variables" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **Some CI jobs failed. Please review the errors above.**" >> $GITHUB_STEP_SUMMARY
        fi
